name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  # ============================================================================
  # Core Tests
  # ============================================================================
  test:
    name: Test Suite
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build UI
        run: |
          cd crates/surgedb-server/ui
          npm install
          npm run build

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ matrix.rust }}-${{ hashFiles('**/Cargo.lock') }}

      - name: Run tests
        run: cargo test --workspace --all-features

  # ============================================================================
  # Linting & Formatting
  # ============================================================================
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

  # ============================================================================
  # WASM Build Check
  # ============================================================================
  wasm:
    name: WASM Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Build WASM
        run: wasm-pack build crates/surgedb-wasm --target web


#   # ============================================================================
#   # Android/iOS Cross-Compilation Checks
#   # ============================================================================
#   cross-compile:
#     name: Cross-Compile ${{ matrix.target }}
#     runs-on: ${{ matrix.os }}
#     strategy:
#       fail-fast: false
#       matrix:
#         include:
#           # Android
#           - target: aarch64-linux-android
#             os: ubuntu-latest
#           - target: armv7-linux-androideabi
#             os: ubuntu-latest
#           - target: x86_64-linux-android
#             os: ubuntu-latest
#           # iOS
#           - target: aarch64-apple-ios
#             os: macos-latest
#           - target: aarch64-apple-ios-sim
#             os: macos-latest
#           - target: x86_64-apple-ios
#             os: macos-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Install Rust
#         uses: dtolnay/rust-toolchain@stable
#         with:
#           targets: ${{ matrix.target }}

#       - name: Install Android NDK
#         if: contains(matrix.target, 'android')
#         uses: nttld/setup-ndk@v1
#         id: setup-ndk
#         with:
#           ndk-version: r26b

#       - name: Set Android linker
#         if: contains(matrix.target, 'android')
#         run: |
#           mkdir -p .cargo
#           cat >> .cargo/config.toml << EOF
#           [target.aarch64-linux-android]
#           linker = "${{ steps.setup-ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android24-clang"
          
#           [target.armv7-linux-androideabi]
#           linker = "${{ steps.setup-ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin/armv7a-linux-androideabi24-clang"
          
#           [target.x86_64-linux-android]
#           linker = "${{ steps.setup-ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin/x86_64-linux-android24-clang"
#           EOF

#       - name: Check build (core library)
#         run: cargo check -p surgedb-core --target ${{ matrix.target }} --no-default-features --features simd

#   # ============================================================================
#   # Minimum Supported Rust Version
#   # ============================================================================
#   msrv:
#     name: MSRV Check
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Install Rust 1.70
#         uses: dtolnay/rust-toolchain@master
#         with:
#           toolchain: "1.70"

#       - name: Check MSRV
#         run: cargo check --workspace
